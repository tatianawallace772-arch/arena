/** Content written by /init. Derived from the virtual repo so it reads like a real CLAUDE.md. */
export const CLAUDE_MD = `# CLAUDE.md

Guidance for Claude Code when working in this repo. Generated by /init on the current
tree; keep it short and true — stale instructions are worse than none.

## What this is

pocketmix: an offline-first mixtape builder. One Node process, one SQLite file, no
framework. \`src/routes.js\` is the whole web layer.

## Commands

| Task | Command |
|---|---|
| dev server | \`npm run dev\` (watch mode, :8787) |
| tests | \`npm test\` (\`node --test\`, no runner dep) |
| lint | \`npm run lint\` |
| deploy | \`scripts/deploy.sh staging\` — runs test + lint first |

Run a single file with \`node --test tests/sort.test.js\`.

## Rules that matter

1. **Never interpolate user input into SQL.** \`src/store.js\` builds query strings; the
   only thing standing between \`?sort=\` and an injection is the allowlist in
   \`src/util/sort.js\`. If you widen \`FIELDS\`, add a test in the same change.
2. **Escape in the renderer, not the caller.** Every user-supplied string goes through
   \`escapeHtml\` at emit time in \`src/render.js\`. Do not add unescaped interpolation.
3. **Sort keys are validated, case-insensitively.** Direction casing is normalised before
   \`DIRS.includes()\` — see the test that exists only because that regressed once.
4. **Track \`position\` is the array index.** \`render.js\` adds 1 for display. Do not sort
   client-side.
5. **No new runtime dependencies** without asking. Node built-ins (\`node:sqlite\`,
   \`node:crypto\`, \`node:test\`) cover what this app needs.

## Layout

    src/server.js   boot, listen, structured log
    src/routes.js   routing + body parsing (1 MB cap)
    src/store.js    SQL (string-built by design — see rule 1)
    src/db.js       open + schema, CREATE ... IF NOT EXISTS only
    src/render.js   HTML; the escaping boundary
    src/util/       sort allowlist, slug hash, logger

## Testing

Tests import pure functions directly; nothing boots a server. If you need the request
path covered, extract it rather than adding a fake socket.

## Do not

- Edit \`data/*.db\` or commit anything under \`data/\`
- Add an ORM or a router framework
- Turn on \`--dangerously-skip-permissions\` in CI (deploy.sh would run tests unsandboxed)

## PRs

Conventional commits, one logical change each. Title the commit after the *why*:
\`fix(sort): normalize direction casing before validation\`, not \`update sort.js\`.
`;
