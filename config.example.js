/* Example configuration.
 *  1. Copy this file to config.js
 *  2. Paste your API key below (https://fish.audio/app/api-keys/)
 * config.js is gitignored, so your key never lands in the repository.
 */
window.FISH_CONFIG = {
  apiKey: "sk-fish-..."
};
